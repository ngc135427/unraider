# unraider

已实现页面：

- 登录页：服务器协议切换、表单校验、记住我、登录成功反馈
- 注册页：用户名、密码、确认密码校验，成功后返回登录
- 服务器主页：服务器信息、图标选择、关机/重启确认
- Docker、虚拟机、共享列表：底部导航切换
- 详情页：返回、详情分组、确认操作弹窗


```bash
flutter pub get
flutter create --platforms=android,ios,web --project-name unraider .
flutter run
```
